sh setup.sh --> to setup folder

docker-compose up -d --> to start

docker-compose ps --> to check

cd haproxy

nano haproxy.env

edit active and standby server

docker-compose up -d

docker-compose ps
